@inherits LayoutComponentBase
@inject UserStateService UserState
@inject NavigationManager Nav

<MudThemeProvider Theme="_theme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="2" Color="Color.Error" Dense="true">
        <!-- Marca: visible sempre -->
        <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2" Style="min-width:0;flex-shrink:0">
            <MudImage Src="/images/Logo.png" Height="28" Style="filter:brightness(0) invert(1);flex-shrink:0" />
            <MudStack Spacing="0" Class="d-none d-sm-flex">
                <MudText Typo="Typo.subtitle1" Style="font-weight:700;line-height:1.1;color:white">
                    Salesians de Sarrià
                </MudText>
                <MudText Typo="Typo.caption" Style="line-height:1;color:rgba(255,255,255,.8)">
                    Departament d'Informàtica
                </MudText>
            </MudStack>
        </MudStack>
        <MudSpacer />
        @if (UserState.IsLoggedIn)
        {
            <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="1" Style="min-width:0">
                <MudChip T="string" Color="Color.Default" Size="Size.Small"
                         Style="color:white;background:rgba(255,255,255,.15);flex-shrink:0">
                    @UserState.Role
                </MudChip>
                <!-- Nom: truncat i ocult en pantalles molt petites -->
                <MudText Typo="Typo.body2" Style="color:white;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;max-width:140px"
                         Class="d-none d-sm-block">
                    @UserState.NomComplet
                </MudText>
                <MudIconButton Icon="@Icons.Material.Filled.Logout"
                               Color="Color.Inherit"
                               title="@($"Sortir ({UserState.NomComplet})")"
                               OnClick="Logout" />
            </MudStack>
        }
    </MudAppBar>

    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-4 mb-8">
            @Body
        </MudContainer>
    </MudMainContent>

    <MudAppBar Bottom="true" Elevation="0" Dense="true"
               Style="background:var(--mud-palette-background-grey);border-top:1px solid var(--mud-palette-divider)">
        <MudSpacer />
        <MudText Typo="Typo.caption" Style="color:var(--mud-palette-text-secondary)">
            <strong>Salesians de Sarrià</strong> · Departament d'Informàtica &nbsp;|&nbsp;
            AutoCo Avaluació © @DateTime.Now.Year
        </MudText>
        <MudSpacer />
    </MudAppBar>
</MudLayout>

@code {
    private MudTheme _theme = new()
    {
        PaletteLight = new PaletteLight
        {
            Primary       = "#CC0000",
            PrimaryDarken = "#a30000",
            Error         = "#CC0000",
        }
    };

    private void Logout()
    {
        UserState.Logout();
        Nav.NavigateTo("/", forceLoad: false);
    }
}
