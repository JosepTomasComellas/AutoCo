@inject ApiClient Api
@inject UserStateService UserState
@inject ISnackbar Snackbar

<MudDialog>
    <DialogContent>
        <MudText Typo="Typo.body2" Color="Color.Secondary" Class="mb-4">
            Defineix els criteris d'avaluació per a aquesta activitat.
            Cada criteri ha de tenir una clau única (sense espais) i una etiqueta visible.
        </MudText>

        @if (_loading)
        {
            <MudProgressLinear Indeterminate="true" Color="Color.Primary" />
        }
        else
        {
            @for (int i = 0; i < _items.Count; i++)
            {
                var idx = i;
                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="1" Class="mb-2">
                    <MudTextField @bind-Value="_items[idx].Key" Label="Clau"
                                  Variant="Variant.Outlined" Margin="Margin.Dense"
                                  Style="max-width:130px" Disabled="@(_items[idx].IsGlobal)"
                                  HelperText="@(_items[idx].IsGlobal ? "Global" : "")" />
                    <MudTextField @bind-Value="_items[idx].Label" Label="Etiqueta"
                                  Variant="Variant.Outlined" Margin="Margin.Dense" Style="flex:1" />
                    <MudIconButton Icon="@Icons.Material.Filled.ArrowUpward" Size="Size.Small"
                                   Disabled="@(idx == 0)"
                                   OnClick="() => MoveUp(idx)" />
                    <MudIconButton Icon="@Icons.Material.Filled.ArrowDownward" Size="Size.Small"
                                   Disabled="@(idx == _items.Count - 1)"
                                   OnClick="() => MoveDown(idx)" />
                    <MudIconButton Icon="@Icons.Material.Filled.Delete" Size="Size.Small"
                                   Color="Color.Error" Disabled="@(_items.Count <= 1)"
                                   OnClick="() => Remove(idx)" />
                </MudStack>
            }
            <MudButton StartIcon="@Icons.Material.Filled.Add" Size="Size.Small"
                       OnClick="AddItem" Class="mt-2">
                Afegir criteri
            </MudButton>
        }
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="() => MudDialog.Cancel()">Cancel·lar</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="SaveAsync"
                   Disabled="_loading || _saving || !_items.Any()"
                   StartIcon="@Icons.Material.Filled.Save">
            Desar
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] IMudDialogInstance MudDialog { get; set; } = default!;
    [Parameter] public int ActivityId { get; set; }

    private List<CriterionEntry> _items  = [];
    private bool _loading = true;
    private bool _saving  = false;

    private class CriterionEntry
    {
        public string Key      { get; set; } = "";
        public string Label    { get; set; } = "";
        public bool   IsGlobal { get; set; }  // claus originals → no canviem la key per retrocompat.
    }

    protected override async Task OnInitializedAsync()
    {
        Api.SetToken(UserState.Token!);
        var criteria = await Api.GetActivityCriteriaAsync(ActivityId) ?? [];
        _items = criteria.Select(c => new CriterionEntry
        {
            Key      = c.Key,
            Label    = c.Label,
            IsGlobal = false
        }).ToList();
        _loading = false;
    }

    private void AddItem() =>
        _items.Add(new CriterionEntry { Key = $"criteri{_items.Count + 1}", Label = "Nou criteri" });

    private void Remove(int i) { if (_items.Count > 1) _items.RemoveAt(i); }

    private void MoveUp(int i)
    {
        if (i <= 0) return;
        (_items[i], _items[i - 1]) = (_items[i - 1], _items[i]);
    }

    private void MoveDown(int i)
    {
        if (i >= _items.Count - 1) return;
        (_items[i], _items[i + 1]) = (_items[i + 1], _items[i]);
    }

    private async Task SaveAsync()
    {
        // Validació bàsica
        var invalid = _items.Where(x => string.IsNullOrWhiteSpace(x.Key) ||
                                        string.IsNullOrWhiteSpace(x.Label)).ToList();
        if (invalid.Any()) { Snackbar.Add("Tots els criteris han de tenir clau i etiqueta.", Severity.Warning); return; }

        var keys = _items.Select(x => x.Key.Trim().ToLowerInvariant()).ToList();
        if (keys.Distinct().Count() != keys.Count) { Snackbar.Add("Les claus han de ser úniques.", Severity.Warning); return; }

        _saving = true;
        Api.SetToken(UserState.Token!);
        var req = new SaveCriteriaRequest(_items.Select(x =>
            new CriterionItem(x.Key.Trim().ToLowerInvariant(), x.Label.Trim())).ToList());
        var result = await Api.SaveActivityCriteriaAsync(ActivityId, req);
        _saving = false;

        if (result is not null)
        {
            Snackbar.Add("Criteris desats correctament.", Severity.Success);
            MudDialog.Close(DialogResult.Ok(true));
        }
        else Snackbar.Add("Error en desar els criteris.", Severity.Error);
    }
}
